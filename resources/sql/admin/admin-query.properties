selectAllCar = select * from(select row_number() over(order by car_code asc) rnum, c.* from car_list c ) c where rnum between ? and ?

selectTotalContents = select count(*) cnt from car_list

searchCarByCarCode = select * from (select row_number() over(order by car_code asc) rnum, c.* from car_list c where car_code like ?) where rnum between ? and ?

searchCarByCarName = select * from (select row_number() over(order by car_code asc) rnum, c.* from car_list c where car_name like ?) where rnum between ? and ?

searchCarByCarNumberPlate = select * from (select row_number() over(order by car_code asc) rnum, c.* from car_list c where number_plate like ?) where rnum between ? and ?

searchCarCountByCarCode = select count(*) from car_list where car_code like ?

searchCarCountByCarName = select count(*) from car_list where car_name like ?

searchCarCountByCarNumberPlate = select count(*) from car_list where number_plate like ?

selectAllCarInfo = select * from(select row_number() over(order by car_info_no asc) rnum, c.* from car_info c ) c where rnum between ? and ?

searchCarInfoByCarName = select * from (select row_number() over(order by car_info_no asc) rnum, c.* from car_info c where car_name like ?) where rnum between ? and ?

searchCarInfoByMaker = select * from (select row_number() over(order by car_info_no asc) rnum, c.* from car_info c where maker like ?) where rnum between ? and ?

searchCarInfoByFuel = select * from (select row_number() over(order by car_info_no asc) rnum, c.* from car_info c where fuel like ?) where rnum between ? and ?

searchCarInfoByCarSize = select * from (select row_number() over(order by car_info_no asc) rnum, c.* from car_info c where car_size like ?) where rnum between ? and ?

searchCarInfoCountByCarName = select count(*) from car_info where car_name like ?

searchCarInfoCountByMaker = select count(*) from car_info where maker like ?

searchCarInfoCountByFuel = select count(*) from car_info where fuel like ?

searchCarInfoCountByCarSize = select count(*) from car_info where car_size like ?